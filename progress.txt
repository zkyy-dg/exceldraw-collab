## Codebase Patterns
- @hono/node-server 1.19+ does NOT export `upgradeWebSocket`. Use `createAdaptorServer({ fetch })` to get a Node.js http.Server, then add `server.on("upgrade", ...)` with ws.WebSocketServer({ noServer: true })
- Excalidraw does NOT export `PointerUpdate` type. Define locally: `{ x: number; y: number; tool: "pointer" | "laser"; pressure: number; pointerType: string }`
- `window.EXCALIDRAW_ASSET_PATH` is not in Excalidraw's type declarations. Use `declare global { interface Window { EXCALIDRAW_ASSET_PATH: string } }` to avoid TS errors
- Excalidraw's @radix-ui/react-tabs has peer dep warnings for React 19 — harmless, ignore
- pnpm requires `onlyBuiltDependencies` in root package.json to compile native modules like better-sqlite3
- The server runs on PORT env var (default 3100 in this environment, default 3001 in code)
- `tsconfig.base.json` at root with `"moduleResolution": "bundler"` is shared by both packages

---

## 2026/06/07 - US-001
- Verified and fixed pre-built monorepo scaffold to pass all acceptance criteria
- Files modified: packages/client/src/collab/socket.ts, packages/client/src/components/Whiteboard.tsx, packages/server/src/index.ts
- **Learnings for future iterations:**
  - The `@hono/node-server` docs mention `upgradeWebSocket` but it's only available in Bun/Deno/CF adapters. Node.js must use raw `ws` with manual upgrade handling
  - `createAdaptorServer` returns a native `http.Server`, allowing `server.on("upgrade", ...)` for WebSocket
  - Always verify imports against actual package exports, not just docs
  - `better-sqlite3` native build needs `pnpm rebuild` after initial install if build scripts were blocked
---

## 2026/06/07 - US-002
- WebSocket echo server already working from US-001 implementation, added raw echo fallback for non-JSON messages
- Files modified: packages/server/src/index.ts
- **Learnings for future iterations:**
  - The catch block in message handler serves double duty: echo for non-JSON, error logging for parse failures
  - WebSocket echo is useful for connectivity testing — keep it as fallback even after adding protocol handling
---

## 2026/06/07 - US-003
- Database schema and board CRUD API pre-built, verified all endpoints via curl
- All endpoints working: POST/GET/GET:id/PUT:elements/GET:elements, 404 handling, CORS
- Added *.db-shm and *.db-wal to .gitignore (WAL mode creates these files)
- **Learnings for future iterations:**
  - better-sqlite3 supports `.bind().get()` chain — use .get() for RETURNING queries, .run() for mutations
  - SQLite RETURNING clause works with better-sqlite3 .get()
  - DB path resolves relative to compiled output (dist/) — use __dirname derived from import.meta.url
---

## 2026/06/07 - US-004
- WebSocket room management pre-built in ws/room.ts, verified with multi-client integration test
- Test confirmed: JOIN→INIT/USER_JOINED, ELEMENTS_UPDATE/POINTER_UPDATE broadcast to room, disconnect→USER_LEFT
- Files verified: packages/server/src/ws/room.ts, packages/server/src/index.ts
- **Learnings for future iterations:**
  - Room data is in-memory only — will be lost on server restart (acceptable for MVP)
  - broadcastToRoom checks readyState === 1 before sending — prevents errors on half-open connections
---
